---
import Layout from "../../layouts/Layout.astro";
import { groups, meta, componentsInGroup, withBase, groupColor, groupIcon } from "../../lib/site";
---

<Layout
  title="Components · Compose Playground"
  description={`Browse all ${meta.componentCount} Jetpack Compose + Material 3 components across ${meta.groupCount} groups, with pixel-accurate previews and copy-paste Kotlin.`}
  active="components"
>
  <section class="section container">
    <header class="page-head">
      <p class="eyebrow">Component gallery</p>
      <h1><span class="gradient-text">All components</span></h1>
      <p class="lead">
        {meta.componentCount} components across {meta.groupCount} groups — each with
        {meta.exampleCount} copy-paste Kotlin examples in total.
      </p>
    </header>

    <div class="filter-bar" role="search">
      <div class="filter-field">
        <svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="11" cy="11" r="7" /><path d="m21 21-4.3-4.3" />
        </svg>
        <input
          id="component-filter"
          type="search"
          placeholder="Filter components by name or summary…"
          aria-label="Filter components"
          autocomplete="off"
          spellcheck="false"
        />
        <span class="filter-count" data-filter-count aria-live="polite"></span>
      </div>
    </div>

    {/* group jump nav */}
    <nav class="group-nav" aria-label="Jump to group">
      {groups.map((g) => (
        <a class="group-pill" href={`#${g.key}`} style={`--group-accent:${groupColor(g.key)}`}>
          {g.label}<span class="group-pill-n">{componentsInGroup(g.key).length}</span>
        </a>
      ))}
    </nav>

    <p class="no-results" data-no-results hidden>
      No components match <strong data-no-results-term></strong>. Try a different term.
    </p>

    {groups.map((group) => {
      const items = componentsInGroup(group.key);
      const accent = groupColor(group.key);
      return (
        <section id={group.key} class="group-section" data-group-section style={`--group-accent:${accent}`}>
          <div class="group-head">
            <div class="gh-left">
              <span class="group-ic" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" set:html={groupIcon(group.key)} />
              </span>
              <h2 class="group-title">{group.label}</h2>
            </div>
            <span class="group-meta">{items.length} {items.length === 1 ? "component" : "components"}</span>
          </div>
          <p class="group-blurb muted">{group.blurb}</p>

          <div class="card-grid">
            {items.map((c) => (
              <a
                class="card card-hover comp-card"
                href={withBase(`/components/${c.id}`)}
                data-card
                data-name={c.name}
                data-summary={c.summary}
                style={`--group-accent:${accent}`}
              >
                <span class="thumb">
                  <img src={withBase(`/${c.screenshot}`)} alt={`${c.name} preview`} loading="lazy" />
                </span>
                <span class="comp-body">
                  <span class="comp-name">{c.name}</span>
                  <span class="comp-summary">{c.summary}</span>
                  <span class="comp-foot">
                    <span class="comp-count">{c.examples.length} {c.examples.length === 1 ? "example" : "examples"}</span>
                    <svg class="comp-arrow" aria-hidden="true" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                  </span>
                </span>
              </a>
            ))}
          </div>
        </section>
      );
    })}
  </section>
</Layout>

<style>
  .page-head { margin-bottom: 24px; }
  .page-head h1 { margin-bottom: 0.3em; }

  .filter-bar {
    position: sticky; top: calc(var(--header-h) + 8px); z-index: 20;
    margin-bottom: 18px; padding: 8px 0;
    background: var(--header-bg);
    backdrop-filter: blur(8px);
  }
  .filter-field {
    display: flex; align-items: center; gap: 12px; padding: 12px 18px;
    border-radius: 0; background: var(--surface);
    border: 1px solid var(--ink-12); box-shadow: var(--shadow);
  }
  .filter-field:focus-within { border-color: var(--sheen); box-shadow: 0 0 0 2px var(--sheen); }
  .filter-field svg { color: var(--ink-45); flex: none; }
  .filter-field input { flex: 1 1 auto; min-width: 0; background: transparent; border: 0; color: var(--ink); font: inherit; font-size: 1rem; outline: none; }
  .filter-field input::placeholder { color: var(--ink-45); }
  .filter-count { flex: none; font-size: 0.82rem; color: var(--ink-45); font-variant-numeric: tabular-nums; font-family: var(--font-mono); }

  .group-nav { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 40px; }
  .group-pill {
    display: inline-flex; align-items: center; gap: 8px; padding: 7px 14px;
    border-radius: 0; font-size: 0.88rem; font-weight: 600; color: var(--ink-70);
    background: var(--ink-06); border: 1px solid var(--hairline);
  }
  .group-pill:hover { color: var(--ink); border-color: var(--group-accent); text-decoration: none; }
  .group-pill-n { font-size: 0.74rem; color: var(--group-accent); font-variant-numeric: tabular-nums; font-family: var(--font-mono); }

  .no-results { text-align: center; color: var(--ink-70); padding: 48px 12px; font-size: 1.05rem; }

  .group-section { margin-bottom: clamp(40px, 6vw, 64px); scroll-margin-top: calc(var(--header-h) + 72px); }
  .group-section[hidden] { display: none; }
  .group-head { display: flex; align-items: center; justify-content: space-between; gap: 16px; margin-bottom: 4px; }
  .gh-left { display: flex; align-items: center; gap: 12px; }
  .group-ic {
    display: inline-grid; place-items: center; width: 36px; height: 36px; border-radius: 0;
    background: var(--ink-06); border: 1px solid var(--ink-12);
    color: var(--group-accent);
  }
  .group-ic svg { width: 18px; height: 18px; }
  .group-title { margin: 0; }
  .group-meta { flex: none; font-size: 0.85rem; color: var(--ink-45); font-weight: 600; font-family: var(--font-mono); }
  .group-blurb { max-width: 64ch; margin-bottom: 22px; }

  .card-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 16px; }

  .comp-card {
    position: relative; display: flex; flex-direction: column; padding: 0; overflow: hidden; color: var(--ink);
  }
  .comp-card:hover { text-decoration: none; }
  .comp-card[hidden] { display: none; }
  .thumb {
    display: block; aspect-ratio: 16 / 10; overflow: hidden; border-bottom: 1px solid var(--hairline);
    background: var(--surface-strong);
  }
  .thumb img { width: 100%; height: 100%; object-fit: cover; object-position: center; transition: transform .4s ease; }
  .comp-card:hover .thumb img { transform: scale(1.04); }
  .comp-body { display: flex; flex-direction: column; gap: 8px; padding: 16px 18px 18px; flex: 1; }
  .comp-name { font-weight: 700; font-size: 1.08rem; letter-spacing: -0.01em; }
  .comp-summary {
    color: var(--ink-70); font-size: 0.92rem; line-height: 1.5; flex: 1;
    display: -webkit-box; -webkit-line-clamp: 2; line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
  }
  .comp-foot { display: flex; align-items: center; justify-content: space-between; margin-top: 4px; }
  .comp-count {
    font-size: 0.78rem; font-weight: 600; font-family: var(--font-mono); color: var(--group-accent);
    background: var(--ink-06); border: 1px solid var(--ink-12);
    padding: 3px 9px; border-radius: 0;
  }
  .comp-arrow { color: var(--ink-45); transition: transform .2s ease, color .2s ease; }
  .comp-card:hover .comp-arrow { color: var(--group-accent); transform: translateX(3px); }

  @media (max-width: 480px) { .card-grid { grid-template-columns: 1fr; } }
</style>

<script>
  const input = document.getElementById("component-filter") as HTMLInputElement | null;
  const cards = Array.from(document.querySelectorAll<HTMLElement>("[data-card]"));
  const sections = Array.from(document.querySelectorAll<HTMLElement>("[data-group-section]"));
  const countEl = document.querySelector<HTMLElement>("[data-filter-count]");
  const noResults = document.querySelector<HTMLElement>("[data-no-results]");
  const noResultsTerm = document.querySelector<HTMLElement>("[data-no-results-term]");
  const total = cards.length;

  function setCount(visible: number) {
    if (!countEl) return;
    countEl.textContent = visible === total ? `${total}` : `${visible} / ${total}`;
  }

  function applyFilter(raw: string) {
    const term = raw.trim().toLowerCase();
    let visible = 0;
    for (const card of cards) {
      const name = (card.dataset.name ?? "").toLowerCase();
      const summary = (card.dataset.summary ?? "").toLowerCase();
      const match = term === "" || name.includes(term) || summary.includes(term);
      card.hidden = !match;
      if (match) visible++;
    }
    for (const section of sections) {
      section.hidden = !section.querySelector("[data-card]:not([hidden])");
    }
    if (noResults) noResults.hidden = visible !== 0;
    if (noResultsTerm) noResultsTerm.textContent = raw.trim();
    setCount(visible);
  }

  setCount(total);
  input?.addEventListener("input", () => applyFilter(input.value));
</script>
